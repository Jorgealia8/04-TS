export const main = () => {
    console.log('Hola desde scrpit');

    const heading = document.querySelector('h1');
    const parrafos = document.querySelectorAll('p');

    console.dir(heading);
    console.log(parrafos);
};
